#pragma once

// copy this file to secrets.h and fill in your broker
// secrets.h is gitignored

#define MQTT_HOST "192.168.1.100"  // backend IP บน LAN ที่รัน docker mosquitto
#define MQTT_PORT 1883

// Optional: ถ้า define จะข้าม WiFiManager captive portal ไปต่อ WiFi โดยตรง
// ไม่ define = ใช้ captive portal (AP `light-setup-XXXX`)
// #define WIFI_SSID "your-wifi-name"
// #define WIFI_PASSWORD "your-wifi-password"
